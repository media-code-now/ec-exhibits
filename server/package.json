{
  "name": "ec-portal-server",
  "version": "1.0.0",
  "type": "module",
  "description": "Express + Socket.IO server for EC Exhibits client portal prototype with Neon Postgres",
  "main": "index.js",
  "scripts": {
    "start": "npx prisma generate && node index.js",
    "dev": "node index.js",
    "build": "npx prisma generate",
    "setup-db": "npx prisma generate && npx prisma db push",
    "db:studio": "npx prisma studio",
    "db:generate": "npx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0",
    "bcrypt": "^5.1.1",
    "bcryptjs": "^3.0.3",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dayjs": "^1.11.11",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^7.0.13",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "prisma": "^6.1.0"
  }
}
